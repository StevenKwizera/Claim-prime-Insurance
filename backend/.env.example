PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=ksteven
PGDATABASE=cyuzuzo_db
PGADMIN_DATABASE=postgres
PORT=4000

# Optional: evidence files (default ./data/claim-uploads under backend working dir)
# CLAIMS_UPLOAD_DIR=C:/claim-data

# Email for password reset OTPs and staff-account notifications.
# For Gmail, create an App Password and set MAIL_PASSWORD in local.env (no spaces).
MAIL_ENABLED=true
# Address shown to users (brand inbox)
MAIL_FROM=Prime Insurance <primeinsurance@gmail.com>
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
# SMTP login (Gmail App Password). Can differ from MAIL_FROM if "Send mail as" alias is configured.
MAIL_USERNAME=kwizerasteven2000@gmail.com
# MAIL_PASSWORD=your_16_char_app_password
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS=true
FRONTEND_RESET_URL=http://localhost:5173/forgot-password

# JWT (production: set a long random secret, 32+ bytes)
# JWT_SECRET=
